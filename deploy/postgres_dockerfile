# Use the official PostgreSQL image from the Docker Hub
FROM postgres:15

# Set environment variables for PostgreSQL
ENV POSTGRES_USER=aptrs_user
ENV POSTGRES_PASSWORD=databasepassword
ENV POSTGRES_DB=aptrs

EXPOSE 5432

# Start PostgreSQL
CMD ["postgres"]
